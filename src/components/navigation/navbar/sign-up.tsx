import {Button} from "@/components/ui/button";

const SignUp = () => {
  return (
    <div>
      <Button variant="default" size="default" className="btn-primary">
        Sign Up Now
      </Button>
    </div>
  );
};

export default SignUp;
